@model JoeResumeSite.Models.ResumeViewModel
@{
    ViewBag.Title = "Resume";
}

<div class="row">
	<div class="col-sm-1"></div>
	<div class="col-sm-10 resume-holder">
		<iframe src="@Model.Path" class="resume" frameBorder="0"></iframe>

	</div>

</div>
